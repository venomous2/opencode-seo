---
description: Find keywords competitors rank for that you don't (DataForSEO domain intersection)
---

Run the keyword-gap skill for: $ARGUMENTS

Use scripts/dfs_client.py intersection --mode gap against the user's top
competitors (from seo-project.yml or auto-discovered via competitors),
cluster the results, and prioritize by volume x relevance. If $ARGUMENTS is
empty, ask for the user's domain.
